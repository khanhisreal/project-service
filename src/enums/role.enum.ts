export enum Status {
  ACTIVATED = 'Activated',
  INACTIVATED = 'Inactivated',
}

export enum AccountType {
  PREMUIM = 'Premium',
  BASIC = 'Basic',
}

export enum Roles {
  MANAGER = 'Manager',
  LEADER = 'Leader',
  EMPLOYEE = 'Employee',
}
